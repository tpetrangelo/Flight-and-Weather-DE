name: 'flight_weather_dbt'
version: '1.0'
config-version: 2
profile: 'flight_weather_dbt'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  flight_weather_dbt:
    silver:
      +schema: SILVER
    gold:
      +schema: GOLD
