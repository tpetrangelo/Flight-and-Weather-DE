version: 2

sources:
  - name: bronze
    database: FLIGHT_WEATHER        # change only if your DB is different
    schema: BRONZE
    tables:
      - name: FLIGHTS
        identifier: FLIGHTS
      - name: WEATHER
        identifier: WEATHER
